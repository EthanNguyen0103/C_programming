#include <stdio.h>

#define TAXRATE 0.015
int main()
{
  char word[] = {'H','E','L','L','O'};
  // if is too small it will break program
  char word1[7] = {"HELLO!"};

  const char word3[] = {"nguyen"};

 
  printf("%s",word3);
  char s[100];
  s[0] ='h';
  s[1] = 'e';

  printf("\n %s",word1);
}